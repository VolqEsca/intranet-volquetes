<?php
// Configurar cabeceras CORS para todas las peticiones
$origin = $_SERVER['HTTP_ORIGIN'] ?? '';

// Lista de orígenes permitidos
$allowed_origins = [
    'https://hcm7s3-5173.csb.app',
    'http://localhost:5173',
    'http://localhost:3000'
];

// Verificar si el origen está permitido
if (in_array($origin, $allowed_origins)) {
    header("Access-Control-Allow-Origin: $origin");
} else if (preg_match('/https:\/\/.*\.csb\.app$/', $origin)) {
    // Permitir cualquier subdominio de csb.app
    header("Access-Control-Allow-Origin: $origin");
}

header('Access-Control-Allow-Credentials: true');
header('Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS');
header('Access-Control-Allow-Headers: Content-Type, Authorization, X-Requested-With');

// Si es una petición OPTIONS (preflight), responder y terminar
if ($_SERVER['REQUEST_METHOD'] === 'OPTIONS') {
    http_response_code(200);
    exit();
}
?>
